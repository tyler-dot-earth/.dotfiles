# source "${0:A:h}/init.sh"
# WARN: ↑ too simplistic; won't resolve path if symlinked

# %N expands to the path of the file currently being sourced
# this works correctly even when .zshrc itself is a symlink
ZSHRC_FILE="${(%):-%N}"

# :A resolves the path to an absolute, symlink-free location
# :h extracts the directory containing that file
ZSHRC_DIR="${ZSHRC_FILE:A:h}"

# source init.sh relative to the real .zshrc directory
source "$ZSHRC_DIR/init.sh"
